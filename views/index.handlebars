<div class="jumbotron">
    <h1 class="display-4">Bem-vindo ao blog!</h1>
    <p class="lead">Blog desenvolvido com Node.JS, Express e MongoDB</p>
    <hr class="my-4" />
    <p>It uses utility classes for typography and spacing to space content out
        within the larger container.</p>
    {{#if user}}
        <p class="lead">
            <a
                class="btn btn-primary btn-lg"
                href="usuario/logout"
                role="button"
            >Sair</a>
        </p>
    {{else}}
        <p class="lead">
            <a
                class="btn btn-primary btn-lg"
                href="usuario/login"
                role="button"
            >Faça o login</a>
        </p>
        <p class="lead">
            <a
                class="btn btn-primary btn-lg"
                href="usuario/registro"
                role="button"
            >Crie uma conta</a>
        </p>
    {{/if}}
</div>
<hr />
<h2>Postagens recentes:</h2>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">

            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia
                    mais</button></a>
            <hr />
            <small>Categoria: {{categoria.nome}}</small><br />
            <small>Data de publicação: {{data}}</small>

        </div>
    </div>
{{else}}
    <h5>Nenhuma postagem recente</h5>
{{/each}}